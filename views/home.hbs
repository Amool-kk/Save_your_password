<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Password</title>
    <link rel="stylesheet" href="css/home.css">
</head>
<body>
    <nav>
        <h1>Manage Password</h1>
        <ul>
            <a href="/logout">Logout</a>
            {{#if user}}
            <a href="#">Signed In As {{user.username}}</a>
            {{else}}
            <a href="/login">Sign In</a>
            {{/if}}
        </ul>
    </nav>
    {{!-- <div class="msg">
        {{#if msg}}
        <div class="error">{{msg}}</div>
        {{else}}
        <div class="done">{{msg}}</div>
        {{/if}}
    </div> --}}
    <h2>You Can Save your password</h2>
    <section>
        <div class="form">
            <form action="/data-post" method="POST">
                <div class="Url">
                    <label for="url">URL</label>
                    <input type="text" name="url" class="url" placeholder="Enter website Url">
                </div>
                <div class="USERNAME">
                    <label for="USERNAME">UserName</label>
                    <input type="text" name="USERNAME" class="uSERNAME" placeholder="Enter your username">
                </div>
                <div class="Passbox">
                    <label for="passbox">Password</label>
                    <input type="password" name="Spassword" class="password" placeholder="Enter your password">
                </div>

                <button type="submit" class="btn">Add</button>

            </form>
        </div>
    </section>
    <article>
        <div class="viewbox">
            <div class="viewcont">
                {{#if data}}
                {{#each data}}
                <div class="view">
                    <div class="websiteurl items">
                        <h5>Url:- <p name="thisurl">{{this.url}}</p>
                        </h5>
                    </div>
                    <div class="websiteusername items">
                        <h5>Username:- <p name="thisusername">{{this.USERNAME}}</p>
                        </h5>
                    </div>
                    <div class="savepassword items">
                        <h5>Password:- <p name="thisspassword">{{this.Spassword}}</p>
                        </h5>
                    </div>
                    <div class="button items">
                        <form action="/Delete" method="post">
                            <button type="submit" name="deleteBtn" value="{{this.url}}" class="btn">Delete</button>
                        </form>
                    </div>
                </div>
                {{/each}}
            </div>
            {{else}}
            <div class="msg">
                Nothing to show! Use "Add" section above to Save Password with UserName.
            </div>
            {{/if}}
        </div>
    </article>
</body>

</html>